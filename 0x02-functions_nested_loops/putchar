#include <unistd.h>

/**
* _putchar - writes the character c to stdout
* @c: The character to print
*
* RETURN: on success 1.
* On error, -1 is returned, and eror is sent appropriately.
*/
int _putchar(char c)
{
return (write(1, &c, 1));
}
